<h1><%= t :all_article %></h1>

<%= t :countblog1 %><%= Article.public_count %> <%= t :countblog2 %>

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to t(:create), new_article_path %>